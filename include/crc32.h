/* ------------------------------------------------------------------
 * Crc32 Header
 * ------------------------------------------------------------------ */

#ifndef CRC32_H
#define CRC32_H

/* Calculate crc32 checksum */
extern uint32_t crc32buf ( uint8_t * buf, size_t len );

#endif
